# Lua AWS Lambda function example

An AWS Lambda function and included runtime written in Lua. For a talk given at work

## Requirements

AWS SAM 
Docker Desktop

## Usage

```commandline
sam build

sam local invoke [--event ./events/event.json]
```
